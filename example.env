# MySQL
MYSQL_ROOT_PASSWORD=PASSWD
MYSQL_DATABASE=workshop
MYSQL_USER=student
MYSQL_PASSWORD=PASSWD

# Redis
REDIS_PASSWORD=PASSWD

# MinIO (local S3)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=PASSWD

# JupyterHub
JUPYTERHUB_DUMMY_PASSWORD=PASSWD
JUPYTERHUB_ADMIN_USERS=admin
# Optional: override the single-user image
JUPYTER_SPAWN_IMAGE=jupyter/pyspark-notebook:latest

# Host UID for admin user (to match host permissions)
# On macOS: run 'id -u' to get your UID
# On Linux: run 'id -u' to get your UID
HOST_UID=503
HOST_GID=20
